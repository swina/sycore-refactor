Actually the ResultsPanel.vue is always loaded as main content in the body of the app.

- Plan to create from ResultsPanel.vue a new component SoundEngine.vue with the same logic but in a modal (draggable and resizable)
- Plan to add to all modal a minimize function and a suggestion on how to recall minimized modals