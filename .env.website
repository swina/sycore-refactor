# Loaded automatically by Vite for `--mode website` builds (npm run build:website).
# URL of the deployed PWA app — used by the website's "Launch App" buttons.
VITE_APP_URL=https://sycoreapp.vercel.app/
